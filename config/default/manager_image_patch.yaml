apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        # Change the value of image field below to your controller image URL
        - image: iad.ocir.io/odx-mockcustomer/cluster-api-oci-controller-amd64:0.5-alpha-30
          name: manager